Read the deliberation engine skill file at `.claude/skills/deliberate/SKILL.md` and execute it.

Arguments: $ARGUMENTS

If no arguments provided, run in interactive mode with auto-detected tier.
Valid arguments: `greenfield`, `wip`, `polish`, `auto`, `incremental` (can combine: `auto polish`, `auto incremental`).

Start at Step 0. Follow every step in order. Do not skip steps.
