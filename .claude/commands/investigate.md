Root cause investigation — systematic debugging with hypothesis chain.

$ARGUMENTS

Load and follow the **Review Mode** section in: .claude/skills/investigation/SKILL.md

Iron Law: No fix without confirmed root cause.

Focus on:
- Reproduce → Isolate → Hypothesize → Verify → Fix → Confirm
- 3-strike rule: if 3 fixes fail, re-analyze from scratch
- Scope lock: don't fix unrelated issues during investigation
- Output investigation report with hypothesis chain
